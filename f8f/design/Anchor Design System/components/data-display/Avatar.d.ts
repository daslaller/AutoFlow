/** Circular initial avatar with brand gradient, used for assignee/technician chips on Kanban cards. @startingPoint section="Data Display" subtitle="Circular initial avatar" viewport="700x80" */
export interface AvatarProps{ initial:string; size?:number; }
export function Avatar(props: AvatarProps): JSX.Element;
