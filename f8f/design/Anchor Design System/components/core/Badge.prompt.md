Badge — compact label for status, category, or count.
```jsx
<Badge variant="secondary">screen</Badge>
```
Variants: default (solid), secondary (neutral grey), destructive (red, low-stock/cancelled), outline (bordered, tag chips). For semantic work-status colors use the Domain group's StatusBadge instead.